table:
  schema: mergestat
  name: repo_sync_log_types
is_enum: true
array_relationships:
  - name: repo_sync_logs
    using:
      foreign_key_constraint_on:
        column: log_type
        table:
          schema: mergestat
          name: repo_sync_logs
