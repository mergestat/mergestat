table:
  name: repo_import_types
  schema: mergestat
is_enum: true
array_relationships:
- name: repo_imports
  using:
    foreign_key_constraint_on:
      column: type
      table:
        name: repo_imports
        schema: mergestat
