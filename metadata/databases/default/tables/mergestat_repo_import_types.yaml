table:
  schema: mergestat
  name: repo_import_types
is_enum: true
array_relationships:
  - name: repo_imports
    using:
      foreign_key_constraint_on:
        column: type
        table:
          schema: mergestat
          name: repo_imports
