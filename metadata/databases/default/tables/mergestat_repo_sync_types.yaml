table:
  schema: mergestat
  name: repo_sync_types
is_enum: true
array_relationships:
  - name: repo_syncs
    using:
      foreign_key_constraint_on:
        column: sync_type
        table:
          schema: mergestat
          name: repo_syncs
